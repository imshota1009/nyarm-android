# 🐱 Nyarm - スマートアラームアプリ

期間一括設定ができる、猫キャラクター付きアラームアプリです。

![Nyarm Banner](assets/banner.png)

## ✨ 特徴

- **📅 期間一括設定** - カレンダーで期間を選択して、まとめてアラームを設定
- **🚫 除外設定** - 土日や祝日を自動で除外
- **📋 テンプレート** - よく使う設定を保存して再利用
- **🐱 猫キャラクター** - 可愛い猫がアラームを応援！
- **🔔 プッシュ通知** - アラーム時刻に確実にお知らせ

## 📱 インストール方法

### 方法1: APKをダウンロード（推奨）

1. [Releases](../../releases) ページを開く
2. 最新の `nyarm-vX.X.X.apk` をダウンロード
3. Androidスマホでインストール

> ⚠️ 初回インストール時は「提供元不明のアプリ」を許可する必要があります

### 方法2: Expo Goで試す

1. スマホに [Expo Go](https://expo.dev/client) をインストール
2. このリポジトリをクローン
3. `npm install` を実行
4. `npx expo start` を実行
5. 表示されるQRコードをスキャン

## 🛠️ 開発環境のセットアップ

```bash
# リポジトリをクローン
git clone https://github.com/yourusername/nyarm-android.git
cd nyarm-android

# 依存関係をインストール
npm install

# 開発サーバーを起動
npx expo start
```

## 📦 APKのビルド方法

### ローカルでビルド（開発用）

```bash
# EAS CLIをインストール
npm install -g eas-cli

# EASにログイン
eas login

# 開発用APKをビルド
eas build --platform android --profile development
```

### GitHub Actionsで自動ビルド

このリポジトリではRelease作成時に自動でAPKがビルドされます。

## 📁 プロジェクト構成

```
nyarm-android/
├── App.tsx              # メインアプリ
├── src/
│   ├── components/      # UIコンポーネント
│   │   ├── Calendar.tsx
│   │   ├── CatCharacter.tsx
│   │   └── AlarmModal.tsx
│   ├── hooks/           # カスタムフック
│   │   ├── useAlarms.ts
│   │   ├── useTemplates.ts
│   │   └── useNotifications.ts
│   ├── utils/           # ユーティリティ
│   │   ├── storage.ts
│   │   └── date.ts
│   └── types/           # 型定義
│       └── index.ts
├── assets/              # 画像など
├── app.json             # Expo設定
└── package.json
```

## 🎨 使用技術

- **Expo** (React Native)
- **TypeScript**
- **expo-notifications** - プッシュ通知
- **AsyncStorage** - データ永続化

## 📄 ライセンス

MIT License

---

🐱 「Nyarmを使ってくれてありがとうニャ！」
